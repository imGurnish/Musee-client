name: Build and Deploy Flutter Web to Netlify

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.4'
          channel: stable
          cache: true

      - name: Enable Flutter Web
        run: flutter config --enable-web

      - name: Install dependencies
        run: flutter pub get

      - name: Create app_secrets.dart
        shell: bash
        run: |
          mkdir -p lib/core/secrets
          cat > lib/core/secrets/app_secrets.dart <<'EOF'
          class AppSecrets {
            // Environment configuration
            static const bool isProduction = true;

            // Supabase configuration (safe for all platforms)
            static const String supabaseUrl = "${{ secrets.SUPABASE_URL }}";
            static const String supabaseAnonKey = "${{ secrets.SUPABASE_ANON_KEY }}";
            static const String backendUrl = "${{ secrets.BACKEND_URL }}";

            // External Music API
            static const String externalMusicBaseUrl = "${{ secrets.EXTERNAL_MUSIC_BASE_URL }}";
            static const String externalMusicDecryptionKey = "${{ secrets.EXTERNAL_MUSIC_DECRYPTION_KEY }}";
            static const String externalMusicOrigin = "${{ secrets.EXTERNAL_MUSIC_ORIGIN }}";
            static const String externalMusicReferer = "${{ secrets.EXTERNAL_MUSIC_REFERER }}";
          }
          EOF

      - name: Add Netlify redirects for Flutter Web routing
        run: |
          mkdir -p web
          echo "/* /index.html 200" > web/_redirects

      - name: Build Flutter Web
        run: flutter build web --release

      - name: Deploy to Netlify
        uses: nwtgck/actions-netlify@v2.0.0
        with:
          publish-dir: build/web
          production-deploy: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
